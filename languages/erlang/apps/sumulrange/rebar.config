{erl_opts, [no_debug_info]}.
{deps, []}.

{escript_incl_apps,
 [sumulrange]}.
{escript_main_app, sumulrange}.
{escript_name, sumulrange}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
