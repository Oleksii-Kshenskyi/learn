{erl_opts, [no_debug_info]}.
{deps, []}.

{escript_incl_apps,
 [sumulfile]}.
{escript_main_app, sumulfile}.
{escript_name, sumulfile}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
